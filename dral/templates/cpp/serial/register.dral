struct [dral style="lowercase"]register.name[#dral]
{
  constexpr static uint32_t Address = [dral format="{:#010X}"]peripheral.address[#dral] + [dral format="{:#06X}"]register.offset[#dral];
  union
  {
    uint32_t value;
    [dral template="bitfield.dral"]register.fields[#dral]
  };
};

