# Base on the RM0394 STM32 document

#include "common.yaml"

chip: l432xx

peripherals:
  flash:
    address: 0x40022000
    registers:
      acr:
        offset: 0x00
        policy: rw
        fields:

